name: Garder le site éveillé

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visiter le site
        run: curl -sf --max-time 60 https://signale-mayotte.onrender.com/ > /dev/null
